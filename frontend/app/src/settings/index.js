const API_SERVER = `http://127.0.0.1`; //remove the port when deploying to docker
const LEVEL = `/`;
const POST = `post`;
const GET = `get`;
const API_VERSION = `/api/v1/`;

export { API_SERVER, LEVEL, POST, GET, API_VERSION };
